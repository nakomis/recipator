# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
.DS_Store

# Swift Package Manager
.build/
Packages/
Package.resolved
*.xcconfig

# Build artefacts
build/

# App Store screenshots (generated by the capture script)
ios/screenshots/

# On-device model speedrun benchmark — local-only throwaway (~950MB of CoreML
# candidate models). Provenance lives on the recp-19-speedrun branch.
ios/Recipator/Speedrun/

# Planning
PLAN.md

# Secrets — never commit
ios/Shared/Secrets.swift

# CDK (future)
cdk.out/
*.out/

# Coverage
coverage/
lcov.info

# Node (Chrome extension, future)
node_modules/

# Local scripts not for version control
ignored/

# Chrome extension build output
chrome-extension/dist/
chrome-extension/.pkg-build/
